the json just tested under vs2010
if you use in vs2005 you should change a little
because
this usage typename t::type as a parameter type in funcition which is not supported int vs 2005: typename json::complex_types<character>::string_type
string.back() is not added int vs 2005 either
